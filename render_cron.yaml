# render_cron.yaml — TWO cron entries (UTC)
# Morning (7:45 AM PT during DST)
- schedule: "45 14 * * *"
  command: "python tools/daily_digest.py --variant morning --key $VEGA_ADMIN_KEY"
# Afternoon (market wrap ~1:15 PM PT)
- schedule: "15 20 * * *"
  command: "python tools/daily_digest.py --variant afternoon --key $VEGA_ADMIN_KEY"
